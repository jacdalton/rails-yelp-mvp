<div class="container">
<h1>Add a Review</h1>
<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.button :submit %>
<% end %>
</div>
<%= link_to "Cancel", restaurant_path(@restaurant) %>